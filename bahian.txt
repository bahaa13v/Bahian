!apt update && apt upgrade -y
!apt install debootstrap debian-archive-keyring -y
!cd && mkdir debian && debootstrap stable debian http://deb.debian.org/debian/
!cd && for i in /dev /dev/pts /proc /sys; do sudo mount --bind $i debian$i; done
!cd && chroot debian /bin/bash


apt update && apt upgrade -y
apt install locales -y
echo "en_US.UTF-8 UTF-8" >> /etc/locale.gen
locale-gen